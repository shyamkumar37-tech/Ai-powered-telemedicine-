FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Run the dev server. Ensure Vite binds to 0.0.0.0
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
